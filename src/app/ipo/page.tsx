import { IpoBoard } from "@/components/desk/ipo-board";

export const metadata = {
  title: "IPO · Eminent Corpus",
  description: "NSE current, upcoming, and recent IPO issues.",
};

export default function IpoPage() {
  return <IpoBoard />;
}
